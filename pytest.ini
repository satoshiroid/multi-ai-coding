[pytest]
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
# Hard per-test timeout: a hang must fail fast (with all-thread stacks) instead
# of spinning a CI job. ``thread`` method dumps every thread's traceback, which
# pinpoints blocking I/O even when it is off the main thread.
timeout = 60
timeout_method = thread
